apiVersion: v1
kind: Service
metadata:
  name: mcp-weather-auth-service
  namespace: weather
  labels:
    app: mcp-weather-app-auth
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: mcp-weather-app-auth
