apiVersion: v1
kind: Service
metadata:
  name: mcp-weather-service
  labels:
    app: mcp-weather-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: mcp-weather-app