apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flowise
spec:
  tls:
    - hosts:
        - flowise.192.168.49.2.nip.io
  rules:
    - host: flowise.192.168.49.2.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flowise
                port:
                  number: 3000
